---
import Layout from './PageLayout.astro';
const response = await fetch('https://sessionize.com/api/v2/qw8dobow/view/GridSmart');
const schedule = await response.text();

console.log('schedule', schedule);
---

<Layout>
  <h1 class="text-center font-semibold">Schedule</h1>
  <section class="text-center max-w-max bg-sky-400" set:html={schedule} />
</Layout>
